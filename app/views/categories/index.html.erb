<!DOCTYPE html>
<html lang="en">
  <head>
    <title>OverLink Categories</title>
  </head>

<h1 class="on-black" align="center">List of all categories</h1>
<div class="container-fluid">
  <div class="row">
    <nav class="col-xs-12 col-sm-4 col-md-4 col-lg-3">
      <ul class="list-group">
        <li class="list-group-item">
          <h1 align="center">Popular categories
          </h1>
        </li>
        <!-- <li class="list-group-item">
        </li> -->
        <li class="list-group-item">
          <span class="badge">257</span>
          Photograpy
        </li>
      </ul>
    </nav>
    <div class="col-xs-12 col-sm-8 col-md-8 col-lg-9 ">
      <div align="center">
        <%= will_paginate %></div>
          <ul class="listing">
            <div class="container-fluid">
              <div class="row">
                <% @categories.each do |category| %>
                  <div class="col-xs-6 col-sm-6 col-md-4 col-lg-4">
                    <div class="well">
                      <li class="article-title">
                          <h5>
                          <%= link_to "#{category.name}", category_path(category) %>
                          </h5>

                          <h4><small>
                            <%= truncate(category.description, length: 100) %>
                          </small></h4>
                      </li>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </ul>
        <div align="center">
      <%= will_paginate %></div>
    </div>
  </div>
</div>
